# include <stdarg.h>
# include <unistd.h>
# include <stdlib.h>
# include "libft.h"

int	ft_printf(const char	*fmt, ...)
{
	char	c;
	size_t	i;
	va_list	ap;

	va_start(ap, fmt);
	while(*fmt)
	{
		while (*fmt = )
		{
			/* code */
		}
		
		if (c == '%')
		{
			fmt++;
			c =
		}
		fmt++;
	}
	va_end(ap);
	return (rtn);
}
